1. 输入为一个文本文档，要读取文档的第一行和第二行数据.
2. 内建数据类型包括什么.
3. 数组或vektor保存  每一位  的数值.
4. 检索每一位，如果MyProm InputFile的第一个参数为某个值，则对应不同的非法输入检索法则.
5. 全部合法之后，按照MyProm InputFile第一个参数的进制进行加和操作.
6. 读取MyProm InputFile的第二个参数，输出进制参数进行进制转换.


附
1. 对于首位允许+-符，判断加减运算.
2. 要判断第一个出现   非运算符且非0   的位数，例如使+000235变判断为235.

3.文件读取
由于关于文件操作的内容需要到第八章讲到，这里给出一个简单的读取文件方法。可以参考下面网页中的四种不同读取方式，选用适合自己的就行。
https://zhuanlan.zhihu.com/p/395782246

4.命令行参数
可以参考下面网页，我觉得作业需要用到的这个博客都提到了
https://blog.csdn.net/zhizhengguan/article/details/109022694
